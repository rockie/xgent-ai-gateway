---
phase: 18-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gateway/src/types.rs
  - gateway/src/metrics.rs
  - gateway/src/metrics_history.rs
  - gateway/src/http/admin.rs
  - gateway/src/main.rs
  - gateway/src/bin/agent.rs
  - gateway/examples/sample_service.rs
autonomous: true
requirements:
  - TD-01
  - TD-02
must_haves:
  truths:
    - "cargo clippy produces zero warnings"
    - "cargo check produces zero warnings"
    - "All existing tests still pass"
  artifacts:
    - path: "gateway/src/types.rs"
      provides: "impl std::str::FromStr for TaskState, impl Default for TaskId"
    - path: "gateway/src/metrics.rs"
      provides: "impl Default for Metrics"
    - path: "gateway/src/metrics_history.rs"
      provides: "impl Default for MetricsHistory"
    - path: "gateway/src/http/admin.rs"
      provides: "clamp() instead of manual min/max"
    - path: "gateway/src/main.rs"
      provides: "no useless .into() conversion"
    - path: "gateway/src/bin/agent.rs"
      provides: "no unused_assignments warning"
  key_links:
    - from: "gateway/src/types.rs"
      to: "all callers of TaskState::from_str"
      via: "impl FromStr trait replaces inherent method"
      pattern: "impl FromStr for TaskState"
---

<objective>
Fix all clippy and compiler warnings across the gateway crate.

Purpose: Eliminate 7 clippy warnings and 1 compiler warning to establish a clean baseline. This prevents warning accumulation and makes future regressions immediately visible.
Output: Zero-warning `cargo clippy` and `cargo check` output.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix clippy warnings in types.rs, metrics.rs, metrics_history.rs, admin.rs, main.rs</name>
  <files>gateway/src/types.rs, gateway/src/metrics.rs, gateway/src/metrics_history.rs, gateway/src/http/admin.rs, gateway/src/main.rs</files>
  <read_first>gateway/src/types.rs, gateway/src/metrics.rs, gateway/src/metrics_history.rs, gateway/src/http/admin.rs, gateway/src/main.rs</read_first>
  <action>
Fix 6 clippy warnings and 1 compiler warning:

1. **gateway/src/types.rs** — Implement `std::str::FromStr` trait for `TaskState` instead of the inherent `from_str` method. Add:
   ```rust
   impl std::str::FromStr for TaskState {
       type Err = GatewayError;
       fn from_str(s: &str) -> Result<Self, Self::Err> {
           // move existing match body here
       }
   }
   ```
   Remove the existing `pub fn from_str(...)` inherent method. Update callers in the same file's tests to use `.parse::<TaskState>()` or `TaskState::from_str(s)` (with `use std::str::FromStr;` import). Also add `impl Default for TaskId` delegating to `Self::new()`.

2. **gateway/src/metrics.rs** — Add `impl Default for Metrics` delegating to `Self::new()`:
   ```rust
   impl Default for Metrics {
       fn default() -> Self { Self::new() }
   }
   ```

3. **gateway/src/metrics_history.rs** — Add `impl Default for MetricsHistory` delegating to `Self::new()`:
   ```rust
   impl Default for MetricsHistory {
       fn default() -> Self { Self::new() }
   }
   ```

4. **gateway/src/http/admin.rs line 671** — Replace manual clamp:
   Change: `params.page_size.unwrap_or(25).min(50).max(1)`
   To: `params.page_size.unwrap_or(25).clamp(1, 50)`

5. **gateway/src/main.rs line 469** — Remove useless `.into()`:
   Change: `Ok(Err(e)) => return Err(e.into()),`
   To: `Ok(Err(e)) => return Err(e),`
  </action>
  <verify>
    <automated>cd /Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway && cargo clippy --lib -p xgent-gateway 2>&1 | grep -c "warning:" | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo clippy --lib -p xgent-gateway 2>&1` contains zero lines matching "warning:"
    - gateway/src/types.rs contains `impl FromStr for TaskState`
    - gateway/src/types.rs contains `impl Default for TaskId`
    - gateway/src/metrics.rs contains `impl Default for Metrics`
    - gateway/src/metrics_history.rs contains `impl Default for MetricsHistory`
    - gateway/src/http/admin.rs contains `.clamp(1, 50)`
    - gateway/src/main.rs line 469 does not contain `.into()`
  </acceptance_criteria>
  <done>All 5 lib-level clippy warnings resolved. `cargo clippy --lib` produces zero warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Fix agent binary warnings and remove dead code</name>
  <files>gateway/src/bin/agent.rs, gateway/examples/sample_service.rs</files>
  <read_first>gateway/src/bin/agent.rs, gateway/examples/sample_service.rs</read_first>
  <action>
Fix 2 remaining warnings:

1. **gateway/src/bin/agent.rs line 493** — The `has_in_flight = true` assignment on line 493 is never read because it is overwritten by `has_in_flight = false` on line 524 before the next loop iteration reads it. The variable is only read in the `shutdown` select arm. Fix by restructuring: set `has_in_flight = true` before entering the executor, and use a scope or restructure so the assignment is actually meaningful. The simplest correct fix: wrap the task execution in a block that sets it before and clears after, and use an `Arc<AtomicBool>` or move the flag setting before the `tokio::select!`. Actually the simplest fix: since `has_in_flight` is read in the shutdown arm, which races with the message arm, the current code IS correct behavior-wise but the compiler warns because within the message arm itself the value is overwritten. Suppress with `let _ = std::mem::replace(&mut has_in_flight, true);` — no, that doesn't help. The cleanest fix: remove the `has_in_flight = true` on line 493 and instead check `has_in_flight` based on whether we are inside the message processing. Since the select is single-threaded and we only check in the shutdown arm, set it before the `executor.execute()` call and read it in shutdown. The actual fix: the shutdown arm can only fire BETWEEN iterations (before the executor runs or after it completes), not during. So `has_in_flight` is always false when shutdown fires. Remove the variable entirely AND remove the `has_in_flight` parameter from `graceful_drain`. Always pass `false` for has_in_flight since the select loop is single-threaded and shutdown can only fire when no task is in progress.

   Actually, re-reading the code more carefully: the `tokio::select!` CAN cancel the message future if shutdown fires first, but `stream.message()` doesn't execute the task - it just receives. The task execution `executor.execute(&assignment).await` happens AFTER the message is received, within the same arm. Since `select!` only picks one arm per iteration, if shutdown fires, the message arm doesn't run. If the message arm runs, it runs to completion (executor.execute + report_result) before the next select iteration. So `has_in_flight` is indeed never true when shutdown fires.

   Fix: Change the code to not track `has_in_flight` in the select loop. Remove `has_in_flight = true;` (line 493) and `has_in_flight = false;` (line 524). Pass `false` to `graceful_drain` for the `has_in_flight` parameter. Add a comment explaining why: "// In single-threaded select, shutdown can only fire between task executions, so no task is ever in-flight when drain begins."

2. **gateway/examples/sample_service.rs** — Remove the `#[allow(dead_code)]` on line 38 and the `created: Instant` field on line 39 from `AsyncJob` struct since it is never used.
  </action>
  <verify>
    <automated>cd /Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway && cargo clippy 2>&1 | grep -c "warning:" | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo clippy 2>&1` output contains zero lines matching "warning:"
    - `cargo check 2>&1` output contains zero lines matching "warning:"
    - gateway/src/bin/agent.rs does NOT contain `has_in_flight = true`
    - gateway/examples/sample_service.rs does NOT contain `#[allow(dead_code)]`
    - gateway/examples/sample_service.rs does NOT contain `created: Instant`
    - `cargo test -p xgent-gateway --lib 2>&1` shows all tests pass
  </acceptance_criteria>
  <done>All clippy and compiler warnings eliminated. `cargo clippy` and `cargo check` produce zero warnings. All existing tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway
cargo clippy 2>&1 | grep "warning:" | wc -l  # expect 0
cargo check 2>&1 | grep "warning:" | wc -l   # expect 0
cargo test -p xgent-gateway --lib 2>&1        # all pass
```
</verification>

<success_criteria>
- `cargo clippy` produces zero warnings across all targets (lib, bins, examples)
- `cargo check` produces zero warnings
- All existing unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-tech-debt-cleanup/18-01-SUMMARY.md`
</output>
