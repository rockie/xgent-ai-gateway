---
phase: 19-json-payload-format
plan: 03
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - gateway/tests/integration_test.rs
  - gateway/tests/auth_integration_test.rs
  - examples/nodejs-client/cli-client.js
  - examples/nodejs-client/sync-api-client.js
  - examples/nodejs-client/async-api-client.js
  - README.md
  - examples/nodejs-client/README.md
autonomous: true
requirements:
  - EXMP-04
must_haves:
  truths:
    - "Integration tests submit JSON payloads, not base64"
    - "Auth integration tests submit JSON payloads, not base64"
    - "Node.js clients send JSON object payloads"
    - "Node.js clients display JSON results without base64 decoding"
    - "README documents JSON payload contract"
  artifacts:
    - path: "gateway/tests/integration_test.rs"
      provides: "Integration tests with JSON payloads"
      contains: "serde_json"
    - path: "examples/nodejs-client/cli-client.js"
      provides: "CLI client sending JSON payload"
      contains: "message:"
    - path: "README.md"
      provides: "Documentation with JSON payload examples"
      contains: "JSON"
  key_links:
    - from: "gateway/tests/integration_test.rs"
      to: "gateway/src/queue/redis.rs"
      via: "submit_task calls use String payload"
      pattern: "submit_task.*to_string"
    - from: "examples/nodejs-client/cli-client.js"
      to: "gateway/src/http/submit.rs"
      via: "HTTP POST with JSON object payload"
      pattern: "payload.*message"
---

<objective>
Update all tests, Node.js client examples, and documentation to use JSON payloads instead of base64-encoded bytes.

Purpose: Tests must reflect the new JSON payload contract. Node.js examples must work end-to-end without base64 encoding/decoding. Documentation must describe the JSON contract.
Output: All tests pass with `cargo test`. Node.js clients send JSON objects and display JSON results. README describes JSON payload format.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-json-payload-format/19-RESEARCH.md
@.planning/phases/19-json-payload-format/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update integration and auth tests to use JSON payloads</name>
  <read_first>
    - gateway/tests/integration_test.rs
    - gateway/tests/auth_integration_test.rs
  </read_first>
  <files>gateway/tests/integration_test.rs, gateway/tests/auth_integration_test.rs</files>
  <action>
**integration_test.rs:**

1. Remove `use base64::Engine as _;` or similar base64 imports if present.

2. For direct `queue.submit_task()` calls (used in gRPC tests that bypass HTTP):
   - Change `payload: b"hello".to_vec()` to `payload: r#"{"message":"hello"}"#.to_string()`
   - Change `payload: b"work-payload".to_vec()` to `payload: r#"{"data":"work-payload"}"#.to_string()`
   - Change `payload: b"still-works".to_vec()` to `payload: r#"{"data":"still-works"}"#.to_string()`
   - Change `payload: b"payload-a".to_vec()` to `payload: r#"{"data":"payload-a"}"#.to_string()`
   - Change `payload: b"payload-b".to_vec()` to `payload: r#"{"data":"payload-b"}"#.to_string()`

3. For HTTP submit JSON bodies (used in HTTP-level tests):
   - Remove `base64::Engine::encode(...)` wrapping of payloads
   - Change `"payload": payload_b64` to `"payload": {"message": "hello"}` (a JSON object, not a string)
   - Use `serde_json::json!` macro: `"payload": serde_json::json!({"message": "hello world"})`

4. For gRPC `SubmitTaskRequest` construction:
   - Change `payload: b"hello".to_vec()` to `payload: r#"{"message":"hello"}"#.to_string()`

5. For assertions on payload values:
   - Change `assert_eq!(assignment.payload, b"work-payload")` to `assert_eq!(assignment.payload, r#"{"data":"work-payload"}"#)`
   - Similarly for other payload assertions

6. For `ReportResultRequest` construction:
   - Change `result: b"done-result".to_vec()` to `result: r#"{"output":"done-result"}"#.to_string()`

**auth_integration_test.rs:**

1. Remove `use base64::Engine as _;` import if present.

2. Replace ALL instances of base64-encoded payload in HTTP JSON bodies (~12 occurrences):
   Each `"payload": base64::Engine::encode(&base64::engine::general_purpose::STANDARD, b"test")` becomes:
   `"payload": serde_json::json!({"message": "test"})`

   Specific replacements:
   - `b"test"` payloads -> `serde_json::json!({"message": "test"})`
   - `b"hello"` payloads -> `serde_json::json!({"message": "hello"})`
   - `b"tls-test"` payloads -> `serde_json::json!({"message": "tls-test"})`
   - `b"before"` payloads -> `serde_json::json!({"message": "before"})`
   - `b"after-flush"` payloads -> `serde_json::json!({"message": "after-flush"})`
   - `b"recovered"` payloads -> `serde_json::json!({"message": "recovered"})`

3. For direct `queue.submit_task()` or gRPC calls:
   - Change `payload: b"test".to_vec()` to `payload: r#"{"message":"test"}"#.to_string()`

4. Ensure `serde_json` is imported (likely already available from existing `serde_json::json!` usage elsewhere in the file).
  </action>
  <verify>
    <automated>cd /Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway && cargo test -p xgent-gateway --lib 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - gateway/tests/integration_test.rs does NOT contain `base64::Engine::encode`
    - gateway/tests/integration_test.rs does NOT contain `b"hello".to_vec()` in payload context
    - gateway/tests/auth_integration_test.rs does NOT contain `base64::Engine::encode`
    - gateway/tests/auth_integration_test.rs does NOT contain `b"test".to_vec()` in payload context
    - `cargo test -p xgent-gateway --lib` passes (unit tests)
  </acceptance_criteria>
  <done>All integration and auth tests use JSON string payloads. No base64 encoding in test payload construction. Unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update Node.js clients and documentation for JSON payloads</name>
  <read_first>
    - examples/nodejs-client/cli-client.js
    - examples/nodejs-client/sync-api-client.js
    - examples/nodejs-client/async-api-client.js
    - examples/nodejs-client/README.md
    - README.md
  </read_first>
  <files>examples/nodejs-client/cli-client.js, examples/nodejs-client/sync-api-client.js, examples/nodejs-client/async-api-client.js, examples/nodejs-client/README.md, README.md</files>
  <action>
**cli-client.js:**
1. Change payload from plain string to JSON object:
   - Before: `payload: payload` (where payload is a string like "Hello from Node.js client!")
   - After: `payload: { message: payload }` (a JSON object)
2. Remove base64 result decoding:
   - Before: `console.log(Buffer.from(task.result, 'base64').toString());`
   - After: `console.log(JSON.stringify(task.result, null, 2));`
3. Update the usage comment at top: remove any "base64" references
4. If there's an explicit `Buffer.from` import or usage for base64, remove it

**sync-api-client.js:**
1. Same payload change: `payload: payload` -> `payload: { message: payload }`
2. Same result change: Remove `Buffer.from(..., 'base64')` decoding, use `JSON.stringify(task.result, null, 2)` or direct access
3. Update comments

**async-api-client.js:**
1. Same payload change: `payload: payload` -> `payload: { message: payload }`
2. Same result change: Remove base64 decoding, display JSON result directly
3. Update comments

**examples/nodejs-client/README.md:**
1. Update any code examples showing payload format -- use JSON objects
2. Remove any mention of "base64" encoding/decoding
3. Update the flow description if it mentions base64

**README.md:**
1. Find the API example section (around line 221) showing:
   ```json
   "payload": "aGVsbG8gd29ybGQ="
   ```
   Change to:
   ```json
   "payload": {"message": "hello world"}
   ```
2. Remove line 227: "Payload is base64-encoded opaque bytes. The gateway never interprets payload content."
   Replace with: "Payload is any valid JSON value (object, array, string, number, boolean, null). The gateway treats payloads as opaque -- it stores and forwards them without inspection."
3. Search for any other "base64" references in the context of payloads and update them. Note: do NOT change base64 references related to password hashing or other non-payload uses.
  </action>
  <verify>
    <automated>cd /Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway && grep -r "Buffer.from.*base64" examples/nodejs-client/ ; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - examples/nodejs-client/cli-client.js contains `payload: { message:` (JSON object payload)
    - examples/nodejs-client/cli-client.js does NOT contain `Buffer.from` with `base64`
    - examples/nodejs-client/sync-api-client.js contains `payload: { message:` (JSON object payload)
    - examples/nodejs-client/sync-api-client.js does NOT contain `Buffer.from` with `base64`
    - examples/nodejs-client/async-api-client.js contains `payload: { message:` (JSON object payload)
    - examples/nodejs-client/async-api-client.js does NOT contain `Buffer.from` with `base64`
    - README.md contains `"payload": {"message":` or similar JSON object example
    - README.md does NOT contain `aGVsbG8gd29ybGQ=`
    - README.md contains "any valid JSON value" or equivalent description
  </acceptance_criteria>
  <done>All 3 Node.js clients send JSON object payloads and display JSON results without base64. README documents JSON payload contract with example. No base64 references remain in payload documentation.</done>
</task>

</tasks>

<verification>
- `cargo test -p xgent-gateway --lib` passes (unit tests with new String types)
- `grep -r "base64" examples/nodejs-client/` returns no results
- `grep "aGVsbG8" README.md` returns no results
- README.md describes JSON payload format
</verification>

<success_criteria>
- All test files use JSON string payloads, no base64
- All 3 Node.js clients send JSON objects and read JSON results
- README describes JSON payload contract
- Unit tests pass
- No base64 references remain in payload-related code or docs
</success_criteria>

<output>
After completion, create `.planning/phases/19-json-payload-format/19-03-SUMMARY.md`
</output>
