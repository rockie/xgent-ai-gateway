---
phase: 09-service-and-node-management
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - admin-ui/src/routes/_authenticated/services.$name.tsx
  - admin-ui/src/components/node-table.tsx
  - admin-ui/src/components/deregister-dialog.tsx
autonomous: true
requirements: [SVC-02, SVC-04, NODE-01, NODE-02]

must_haves:
  truths:
    - "Admin can view service detail page with config section and node table at /services/$name"
    - "Admin can see per-service node list with health status, in-flight tasks, drain status, and last seen"
    - "Admin can deregister a service via a destructive confirmation dialog"
    - "Breadcrumb navigation allows returning to service list"
  artifacts:
    - path: "admin-ui/src/routes/_authenticated/services.$name.tsx"
      provides: "Service detail page with config + node table + deregister"
      contains: "useServiceDetail"
    - path: "admin-ui/src/components/node-table.tsx"
      provides: "Node health table with status indicators"
      exports: ["NodeTable"]
    - path: "admin-ui/src/components/deregister-dialog.tsx"
      provides: "Destructive confirmation dialog for service deregistration"
      exports: ["DeregisterDialog"]
  key_links:
    - from: "admin-ui/src/routes/_authenticated/services.$name.tsx"
      to: "/v1/admin/services/{name}"
      via: "useServiceDetail hook"
      pattern: "useServiceDetail\\("
    - from: "admin-ui/src/components/deregister-dialog.tsx"
      to: "/v1/admin/services/{name}"
      via: "useDeregisterService mutation"
      pattern: "useDeregisterService\\(\\)"
    - from: "admin-ui/src/components/node-table.tsx"
      to: "admin-ui/src/components/health-badge.tsx"
      via: "HealthBadge import for node health display"
      pattern: "HealthBadge"
---

<objective>
Build the service detail page with config display, node health table, breadcrumb navigation, and deregister confirmation flow.

Purpose: Delivers SVC-02 (view service detail), SVC-04 (deregister with confirmation), NODE-01 (node list with health), and NODE-02 (node details with in-flight, drain, last seen). Completes all Phase 9 requirements.

Output: Working service detail page at /services/$name with full config, live node table, and deregister action.
</objective>

<execution_context>
@/Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-service-and-node-management/09-CONTEXT.md
@.planning/phases/09-service-and-node-management/09-RESEARCH.md
@.planning/phases/09-service-and-node-management/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 lib/services.ts (created in 09-01) -->

```typescript
// Types
export interface ServiceResponse {
  name: string; description: string; created_at: string;
  task_timeout_secs: number; max_retries: number; max_nodes: number | null;
  node_stale_after_secs: number; drain_timeout_secs: number;
}
export interface NodeStatusResponse {
  node_id: string; health: string; last_seen: string;
  in_flight_tasks: number; draining: boolean;
}
export interface ServiceDetailResponse extends ServiceResponse {
  nodes: NodeStatusResponse[];
}

// Hooks
export function useServiceDetail(name: string): UseQueryResult<ServiceDetailResponse>
export function useDeregisterService(): UseMutationResult
export function deriveServiceHealth(nodes: NodeStatusResponse[]): 'healthy' | 'degraded' | 'down' | 'unknown'
export function relativeTime(isoString: string): string
```

<!-- From Plan 01 health-badge.tsx (created in 09-01) -->
```typescript
export function HealthBadge({ status, draining }: { status: string; draining?: boolean }): JSX.Element
```

<!-- Existing UI components from Phase 8 -->
```typescript
// admin-ui/src/components/error-alert.tsx
export function ErrorAlert({ message, onRetry }: { message: string; onRetry: () => void })

// admin-ui/src/components/page-skeleton.tsx
export function PageSkeleton({ lines, withHeader }: { lines?: number; withHeader?: boolean })
```

<!-- shadcn components installed in Plan 01 -->
// admin-ui/src/components/ui/table.tsx - Table, TableBody, TableCell, TableHead, TableHeader, TableRow
// admin-ui/src/components/ui/breadcrumb.tsx - Breadcrumb, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator
// admin-ui/src/components/ui/alert-dialog.tsx - AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create node table and deregister dialog components</name>
  <files>
    admin-ui/src/components/node-table.tsx,
    admin-ui/src/components/deregister-dialog.tsx
  </files>
  <read_first>
    admin-ui/src/lib/services.ts,
    admin-ui/src/components/health-badge.tsx,
    admin-ui/src/components/ui/table.tsx,
    admin-ui/src/components/ui/alert-dialog.tsx,
    admin-ui/src/components/empty-state.tsx
  </read_first>
  <action>
**1. Create `admin-ui/src/components/node-table.tsx`** (per D-09, D-10, D-11, NODE-01, NODE-02):

Props: `{ nodes: NodeStatusResponse[] }` (import type from `@/lib/services`).

When `nodes.length === 0`: render `<EmptyState icon={Server} heading="No nodes connected" description="Nodes will appear here once they connect to this service." />` (import EmptyState and Server icon from lucide-react).

When nodes exist: render a shadcn Table with columns per D-09:
- **Node ID**: `node.node_id` displayed in monospace font (`font-mono text-sm`). Truncate long IDs with `truncate max-w-[200px]` and show full ID in a title attribute.
- **Health**: render `<HealthBadge status={node.health} draining={node.draining} />` from `@/components/health-badge`. This handles the draining override and unhealthy-to-Stale mapping per D-10.
- **In-flight Tasks**: `node.in_flight_tasks` as a number.
- **Last Seen**: `relativeTime(node.last_seen)` from `@/lib/services` showing relative time like "30s ago", "5m ago". Add `title={node.last_seen}` on the cell for full RFC3339 timestamp on hover.

Table header row: Node ID | Health | In-flight Tasks | Last Seen.
Use `<Table>`, `<TableHeader>`, `<TableRow>`, `<TableHead>`, `<TableBody>`, `<TableCell>` from `@/components/ui/table`.

**2. Create `admin-ui/src/components/deregister-dialog.tsx`** (per D-08, SVC-04):

Props: `{ serviceName: string; open: boolean; onOpenChange: (open: boolean) => void }`.

Uses shadcn AlertDialog (not Dialog — AlertDialog is for destructive confirmations).

Structure:
```
AlertDialog open={open} onOpenChange={onOpenChange}
  AlertDialogContent
    AlertDialogHeader
      AlertDialogTitle: "Deregister service?"
      AlertDialogDescription: "This will permanently remove "{serviceName}" and disconnect all its nodes. This action cannot be undone."
    AlertDialogFooter
      AlertDialogCancel: "Cancel"
      AlertDialogAction (destructive): "Deregister" / "Deregistering..." when pending
```

Logic:
- Call `useDeregisterService()` to get the mutation.
- On AlertDialogAction click: call `deregisterMutation.mutate(serviceName, { onSuccess: () => { onOpenChange(false); router.navigate({ to: '/services' }) } })`.
- Import `useRouter` from `@tanstack/react-router` for navigation back to service list after deregister.
- The AlertDialogAction button should have `className="bg-destructive text-destructive-foreground hover:bg-destructive/90"` for destructive styling.
- Disable the action button while `deregisterMutation.isPending`.
- Show "Deregistering..." text on the action button while pending.
  </action>
  <verify>
    <automated>cd /Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway/admin-ui && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - admin-ui/src/components/node-table.tsx contains `export function NodeTable(`
    - admin-ui/src/components/node-table.tsx contains `HealthBadge`
    - admin-ui/src/components/node-table.tsx contains `relativeTime(`
    - admin-ui/src/components/node-table.tsx contains `Node ID`
    - admin-ui/src/components/node-table.tsx contains `In-flight Tasks` or `In-Flight Tasks`
    - admin-ui/src/components/node-table.tsx contains `Last Seen`
    - admin-ui/src/components/node-table.tsx contains `No nodes connected`
    - admin-ui/src/components/deregister-dialog.tsx contains `export function DeregisterDialog(`
    - admin-ui/src/components/deregister-dialog.tsx contains `useDeregisterService(`
    - admin-ui/src/components/deregister-dialog.tsx contains `Deregister service`
    - admin-ui/src/components/deregister-dialog.tsx contains `cannot be undone`
    - admin-ui/src/components/deregister-dialog.tsx contains `router.navigate`
    - admin-ui/src/components/deregister-dialog.tsx contains `/services`
    - npm run build exits 0
  </acceptance_criteria>
  <done>Node table renders health status with colored badges, in-flight tasks, and relative last-seen times. Deregister dialog shows destructive confirmation and navigates back to list on success. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build service detail page with config, node table, breadcrumbs, and deregister</name>
  <files>
    admin-ui/src/routes/_authenticated/services.$name.tsx
  </files>
  <read_first>
    admin-ui/src/lib/services.ts,
    admin-ui/src/components/node-table.tsx,
    admin-ui/src/components/deregister-dialog.tsx,
    admin-ui/src/components/health-badge.tsx,
    admin-ui/src/components/error-alert.tsx,
    admin-ui/src/components/page-skeleton.tsx,
    admin-ui/src/components/ui/breadcrumb.tsx,
    admin-ui/src/routes/_authenticated/services.tsx
  </read_first>
  <action>
Create `admin-ui/src/routes/_authenticated/services.$name.tsx` as a new TanStack Router dynamic route (per D-05, D-06, D-07, D-08).

**IMPORTANT:** The file name must be `services.$name.tsx` (with dot, not slash) — this is TanStack Router's file-based convention for dynamic route params. The route path will be `/_authenticated/services/$name`.

Route definition:
```typescript
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_authenticated/services/$name')({
  component: ServiceDetailPage,
})
```

**ServiceDetailPage component:**

1. Extract params: `const { name } = Route.useParams()`
2. Fetch data: `const { data, isLoading, isError, error, refetch } = useServiceDetail(name)`
3. State: `const [deregisterOpen, setDeregisterOpen] = useState(false)`

4. **Breadcrumb navigation** (per D-07): At the top of the page, render:
```
Breadcrumb > BreadcrumbList >
  BreadcrumbItem > BreadcrumbLink (asChild) > Link to="/services" > "Services"
  BreadcrumbSeparator
  BreadcrumbItem > BreadcrumbPage > {name}
```
Import `Link` from `@tanstack/react-router` for the breadcrumb link. Use shadcn Breadcrumb components from `@/components/ui/breadcrumb`.

5. **Page header**: Below breadcrumb, flex row with:
   - Left: service name as h1 (text-xl font-semibold) + health badge `<HealthBadge status={deriveServiceHealth(data.nodes)} />` next to the name.
   - Right: "Deregister" Button with Trash2 icon, variant="destructive", size="sm". onClick: `setDeregisterOpen(true)` (per D-08).

6. **Loading/Error states**:
   - `isLoading`: render `<PageSkeleton lines={8} />`
   - `isError`: render `<ErrorAlert message={error.message} onRetry={refetch} />`

7. **Service config section** (per D-06, SVC-02): Render service configuration as a definition list or grid of label-value pairs. Use a 2-column grid (`grid grid-cols-2 gap-4`) for the config fields:
   - Description: `data.description || "No description"`
   - Created: `new Date(data.created_at).toLocaleString()`
   - Task Timeout: `${data.task_timeout_secs}s`
   - Max Retries: `${data.max_retries}`
   - Max Nodes: `${data.max_nodes ?? 'Unlimited'}`
   - Node Stale After: `${data.node_stale_after_secs}s`
   - Drain Timeout: `${data.drain_timeout_secs}s`

   Each item: label in text-sm text-muted-foreground, value in text-sm font-medium. Wrap the whole section in a Card (CardHeader with "Configuration" title, CardContent with the grid).

8. **Node table section** (per D-06, D-09, NODE-01, NODE-02): Below the config card, render:
   - Section heading: "Nodes" as h2 (text-lg font-semibold) with node count badge `({data.nodes.length})`.
   - `<NodeTable nodes={data.nodes} />`

9. **Deregister dialog**: `<DeregisterDialog serviceName={name} open={deregisterOpen} onOpenChange={setDeregisterOpen} />`

Full page layout: `<div className="space-y-6">` containing breadcrumb, header row, config card, nodes section, and deregister dialog.
  </action>
  <verify>
    <automated>cd /Users/rockie/Documents/GitHub/xgent/xgent-ai-gateway/admin-ui && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - admin-ui/src/routes/_authenticated/services.$name.tsx exists
    - services.$name.tsx contains `createFileRoute('/_authenticated/services/$name')`
    - services.$name.tsx contains `useServiceDetail(`
    - services.$name.tsx contains `Route.useParams()`
    - services.$name.tsx contains `Breadcrumb` (breadcrumb navigation per D-07)
    - services.$name.tsx contains `Services` (breadcrumb link text)
    - services.$name.tsx contains `NodeTable`
    - services.$name.tsx contains `DeregisterDialog`
    - services.$name.tsx contains `deriveServiceHealth(`
    - services.$name.tsx contains `HealthBadge`
    - services.$name.tsx contains `Configuration`
    - services.$name.tsx contains `task_timeout_secs`
    - services.$name.tsx contains `Deregister` (button text)
    - services.$name.tsx contains `Trash2` (destructive button icon)
    - npm run build exits 0
  </acceptance_criteria>
  <done>Service detail page shows breadcrumb navigation, service config card, node health table, service-level health badge, and deregister button with confirmation dialog. All Phase 9 requirements (SVC-01 through SVC-04, NODE-01, NODE-02) are complete. Build passes.</done>
</task>

</tasks>

<verification>
- `cd admin-ui && npm run build` passes (TypeScript + Vite)
- Service detail page at /services/$name renders config and node table
- Breadcrumb shows "Services > {name}" with working back-link
- Node table shows all 4 columns: Node ID, Health, In-flight Tasks, Last Seen
- Deregister dialog shows destructive confirmation and navigates back to /services
- Health badges consistent between service-level (healthy/degraded/down) and node-level (healthy/stale/disconnected/draining)
</verification>

<success_criteria>
- Admin can click a service card and see the detail page with config and nodes (SVC-02)
- Admin can deregister a service via the destructive confirmation dialog (SVC-04)
- Admin can see per-service node list with health status indicators (NODE-01)
- Admin can see node details: in-flight tasks, drain status, last seen time (NODE-02)
- Breadcrumb navigation allows returning to service list (D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/09-service-and-node-management/09-02-SUMMARY.md`
</output>
